// Copy this file to mapboxToken.ts and fill in your public token.
// mapboxToken.ts is gitignored — never commit the real token.
// Get your public token at https://account.mapbox.com/
export const MAPBOX_PUBLIC_TOKEN = 'pk.YOUR_MAPBOX_PUBLIC_TOKEN_HERE';
